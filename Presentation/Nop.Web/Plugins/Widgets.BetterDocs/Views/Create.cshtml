@*@model Wombit.Plugin.Widgets.BetterDocs.Models.DocumentModel
@{
    Layout = "~/Areas/Admin/Views/Shared/_AdminPopupLayout.cshtml";
}

<form asp-controller="BetterDocs" asp-action="Create"
      asp-route-btnId="@Context.Request.Query["btnId"]"
      asp-route-formId="@Context.Request.Query["formId"]">
    <div class="content-header clearfix">
        <h1 class="float-left">
            @T("Admin.Common.AddNew")
        </h1>
        <div class="float-right">
            <button type="submit" name="save" class="btn btn-primary">
                <i class="far fa-save"></i>
                @T("Admin.Common.Save")
            </button>
        </div>
    </div>

    <div class="content">
        <div class="form-horizontal">
             @await Html.PartialAsync("_CreateOrUpdate.cshtml", Model)
        </div>
    </div>
</form>*@


@model Wombit.Plugin.Widgets.BetterDocs.Models.DocumentModel
@{
    ViewBag.PageTitle = T("Admin.Common.AddNew").Text;
    //active menu item (system name)
    Html.SetActiveMenuItemSystemName("Documents");
}

<form asp-controller="Category" asp-action="Create" method="post" id="category-form">
    <div class="content-header clearfix">
        <h1 class="float-left">
            @T("Admin.Common.AddNew")
            <small>
                <i class="fas fa-arrow-circle-left"></i>
                <a asp-action="Configure">@T("Wombit.Document.BackToList")</a>
            </small>
        </h1>
        <div class="float-right">
            <button type="submit" name="save" class="btn btn-primary">
                <i class="far fa-save"></i>
                @T("Admin.Common.Save")
            </button>
            <button type="submit" name="save-continue" class="btn btn-primary">
                <i class="far fa-save"></i>
                @T("Admin.Common.SaveContinue")
            </button>
        </div>
    </div>
    @await Html.PartialAsync("_CreateOrUpdate", Model)
</form>